# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
#    parameter_name: value

services:
    CurlService:
        class: anlutro\cURL\cURL
    MorphAdornerService:
        class: AppBundle\Services\MorphAdornerService
#        arguments: ["@CurlService", "plain_value", "%parameter_name%"]
        arguments: ["@CurlService"]
